export const metadata = {
  title: 'Quick Buy 工具查價 | Snap-on 原廠授權',
  description: '搜尋 120,000+ Snap-on 工具產品，即時查看牌價，一鍵詢問優惠',
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};

export default function LiffLayout({ children }) {
  return <>{children}</>;
}
